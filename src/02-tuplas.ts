const prices: ( number | string )[] = [1,42,523,2,6 , "Alexis"];

// Tupla
const user: [string, number, string] = ["alexdev2808", 22, "MX"];

// Desestructuracion tupla
const [username, age] = user;

console.log(username);
console.log(age);
